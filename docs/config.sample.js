// Copy this file to docs/config.js (ignored by git) and fill in your deployment settings.
window.CONFIG = {
  // Supabase Edge Function base URL (e.g., "https://your-project.functions.supabase.co")
  edgeBase: "/functions/v1",
  // Optional headers to send with every Edge request.
  headers: {
    // Authorization: "Bearer YOUR_SUPABASE_ANON_KEY",
  },
};
